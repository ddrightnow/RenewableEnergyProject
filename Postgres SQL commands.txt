npm i npm@latest -g


({onRouteChange}) =>{

https://secure.i.telegraph.co.uk/multimedia/archive/03249/archetypal-female-_3249633c.jpg

https://beautyhealthtips.in/wp-content/uploads/2017/09/face_shine_glow.jpg
==

Object.assign(this.state.user, {entries: count})


CREATE TABLE table_name (column_1 datatype, column_2 datatype, column_3 datatype);
\ test 2 -> change db
\d   -> view table
\q -> quit

INSERT INTO table (column1, column2, ... ) VALUES ('are'.31.'100-12-12')
alter table users add score smallint;

update users set score =100 WHERE name = 'hi'or 'Dare'
test2=# select avg(score) from users;
         avg
---------------------
 66.6666666666666667
(1 row)


test2=# select sum(age) from users;
 sum
-----
 237
(1 row)


test2=# select count(name) from users;


create table login (ID serial NOT NULL PRIMARY KEY, secret VARCHAR (100) NOT NULL, name text UNIQUE NOT NULL);

insert into login (secret, name) values ('abc', 'Dari');

======= db smartbrain tables=======
create table users (
ID serial NOT NULL PRIMARY KEY,
name VARCHAR (100),
email text UNIQUE NOT NULL,
entries BIGINT DEFAULT 0,
joined TIMESTAMP NOT NULL
);


create table login(
ID serial PRIMARY KEY,
hash VARCHAR (100) NOT NULL,
email text UNIQUE NOT NULL,
);





